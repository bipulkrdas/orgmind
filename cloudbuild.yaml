steps:
  # Build the backend service
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Build Backend'
    args:
      - 'build'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/orgmind-webapp-docker-images/orgmind-backend:$COMMIT_SHA'
      - '.'
      - '-f'
      - 'Dockerfile'
    dir: 'backend'

  # Build the frontend service
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Build Frontend'
    dir: 'frontend'
    secretEnv: ['ORG_MIND_NEXT_PUBLIC_OAUTH_REDIRECT_URL']
    entrypoint: 'bash'
    args:
    - '-c'
    - |
      docker build \
        --build-arg "NEXT_PUBLIC_OAUTH_REDIRECT_URL=$$ORG_MIND_NEXT_PUBLIC_OAUTH_REDIRECT_URL" \
        -t "us-central1-docker.pkg.dev/$PROJECT_ID/orgmind-webapp-docker-images/orgmind-frontend:$COMMIT_SHA" \
        -f Dockerfile \
        .

  # Push the backend service image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Push Backend'
    args: ['push', 'us-central1-docker.pkg.dev/$PROJECT_ID/orgmind-webapp-docker-images/orgmind-backend:$COMMIT_SHA']
    wait_for: ['Build Backend']

  # Push the frontend service image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Push Frontend'
    args: ['push', 'us-central1-docker.pkg.dev/$PROJECT_ID/orgmind-webapp-docker-images/orgmind-frontend:$COMMIT_SHA']
    wait_for: ['Build Frontend']

  # Deploy the backend service
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'Deploy Backend'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'orgmind-backend'
      - '--image'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/orgmind-webapp-docker-images/orgmind-backend:$COMMIT_SHA'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '8080'
      - '--service-account=dial2ai-webapp-cloudrun@dial2ai.iam.gserviceaccount.com'
      - '--set-secrets=/secrets/.env=orgmind_webapp_golang_backend:latest'
    wait_for: ['Push Backend']

  # Deploy the frontend service
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'Deploy Frontend'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'deploy'
      - 'orgmind-frontend'
      - '--image'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/orgmind-webapp-docker-images/orgmind-frontend:$COMMIT_SHA'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '3000'
      - '--service-account=dial2ai-webapp-cloudrun@dial2ai.iam.gserviceaccount.com'
    wait_for: ['Push Frontend']

images:
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/orgmind-webapp-docker-images/orgmind-backend:$COMMIT_SHA'
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/orgmind-webapp-docker-images/orgmind-frontend:$COMMIT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY

availableSecrets:
  secretManager:
    - versionName: projects/$PROJECT_ID/secrets/ORG_MIND_NEXT_PUBLIC_OAUTH_REDIRECT_URL/versions/latest
      env: ORG_MIND_NEXT_PUBLIC_OAUTH_REDIRECT_URL
